let current=0,step=0,done=new Set();const framework=["GIVEN","ASKED","CONSTRAINTS","CLUES","PATTERN","DATA STRUCTURE","TEMPLATE","TEST"];const $=x=>document.getElementById(x);function render(){const q=questions[current];$("qno").textContent=`${current+1}/${questions.length}`;$("title").textContent=q.title;$("text").textContent=q.text;$("examples").innerHTML=q.examples.map(x=>`<div class="example">${x}</div>`).join("");$("steps").innerHTML=framework.map((x,i)=>`<div class="step ${i<step?"done":i===step?"active":""}"><b>${i+1}. ${x}</b></div>`).join("");$("next").disabled=step===0;$("result").classList.add("hidden");$("start").disabled=false}$("start").onclick=()=>{step=1;$("start").disabled=true;render()};$("next").onclick=()=>{if(step<framework.length-1){step++;render()}else{const q=questions[current];$("pattern").textContent=q.pattern;$("ds").textContent=q.ds;$("complexity").textContent=q.complexity;$("code").textContent=templates[q.pattern];$("result").classList.remove("hidden")}};$("complete").onclick=()=>{done.add(current);$("progress").textContent=`${done.size} / ${questions.length}`;if(current<questions.length-1){current++;step=0;render()}};document.querySelectorAll(".tab").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".view").forEach(x=>x.classList.remove("active"));b.classList.add("active");$(b.dataset.view).classList.add("active")});$("patternGrid").innerHTML=patterns.map(p=>`<article class="pattern"><h3>${p[0]}</h3><p>${p[1]}</p><small>${p[2]} · ${p[3]}</small></article>`).join("");$("templateList").innerHTML=patterns.map(p=>`<article class="template"><h3>${p[0]}</h3><pre><code>${templates[p[0]]||"Template coming soon."}</code></pre></article>`).join("");render();